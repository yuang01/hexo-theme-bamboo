$color-primary = convert(hexo-config('color_scheme.common.theme')); // 默认颜色
$color-primary-shallow = $color-primary + 10 * 8% + 1 * .5deg;
$color-primary-deep = $color-primary - 4 * 8% + 1 * .5deg;
$color-warning = #e6a23c;
$color-danger =  #f66;
$color-success = #67c23a;
$color-info = #409eff;

$color-link = convert(hexo-config('color_scheme.common.link'));
$border = convert(hexo-config('color_scheme.common.border'));
$color-selection = convert(hexo-config('color_scheme.common.selection'));
$color-inner = convert(hexo-config('color_scheme.common.inner'));

$color-pace = convert(hexo-config('color_scheme.common.pace'));

$color-post-header-title = convert(hexo-config('color_scheme.post.headerTitle'));
$color-post-header-title-font-weight = convert(hexo-config('color_scheme.post.headerTitleFontWeight'));
$color-post-copy-background = convert(hexo-config('color_scheme.post.copyBackground'));
$color-post-copy-font = convert(hexo-config('color_scheme.post.copyColor'));
$color-post-code-background = convert(hexo-config('color_scheme.post.codeBackground'));
$color-post-p-light = convert(hexo-config('color_scheme.post.pLight'));
$color-post-p = convert(hexo-config('color_scheme.post.p'));
$color-post-title = convert(hexo-config('color_scheme.post.title'));
$color-post-toc = convert(hexo-config('color_scheme.post.toc'));

$cursor-default = url(hexo-config('cursor.default'));
$cursor-pointer = url(hexo-config('cursor.pointer'));
$cursor-text = url(hexo-config('cursor.text'));

$color-scroll = convert(hexo-config('color_scheme.common.scroll'));

$bg-floor = convert(hexo-config('color_scheme.common.bgFloor'));